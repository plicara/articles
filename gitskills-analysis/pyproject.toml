[project]
name = "skills-analysis"
version = "0.1.0"
description = "Empirical analysis of the GitSkills dataset of agent skills on GitHub"
requires-python = ">=3.12"
dependencies = [
    "markdown>=3.5",
    "duckdb>=1.5",
    "py3langid>=0.3",
    "fast-langdetect>=1.0.1",
]

[tool.uv]
package = false
