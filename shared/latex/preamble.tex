% House preamble for whitepaper PDFs. \input this from <slug>/paper/main.tex.
%
% Brand values below are vendored copies from plicara-brand
% (tokens/print.json). One direction only: change a colour upstream, then
% update the copy here, never the reverse.
%
% The same preamble lives in plicara/articles-workbench (shared/latex/preamble.tex).
% Change them together or not at all.

\usepackage[a4paper, margin=2.8cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage[colorlinks=true, allcolors=plorange]{hyperref}

% plicara-brand tokens/print.json: "orange", the one hot colour.
% Print: CMYK 3/55/93/0 (SWOP separation, delta E 0.55).
\definecolor{plorange}{HTML}{EE8B33}

% Brand faces (Fraunces, Newsreader) exist only as woff2 in the site repo,
% which LaTeX cannot load, so papers set Palatino until installable fonts
% are vendored. Swap for fontspec + the real faces when they are.
\usepackage{newpxtext,newpxmath}

\setlength{\parskip}{0.4em}
